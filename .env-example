# Skopiuj ten plik jako `.env` w katalogu głównym projektu:
#   cp .env-example .env
# (Windows PowerShell: Copy-Item .env-example .env)
#
# Po zmianie wartości zrestartuj serwer deweloperski (`npm run dev`).

# Supabase — Project Settings → API
# URL projektu (np. https://xxxxxxxxxxxx.supabase.co)
VITE_SUPABASE_URL=VITE_SUPABASE_URL=https://fbnovvfakaglpehmyhvt.supabase.co

# Klucz publiczny anon / public (bezpieczny do użycia w frontendzie)
VITE_SUPABASE_ANON_KEY=sb_publishable_KyWZ6ia808Ol374NYduamw_pgN4XDqq

# Hasło admina — opcjonalne; pozwala wejść do globalnego pokoju z uprawnieniami admina
# Zostaw puste, jeśli nie potrzebujesz trybu admina
VITE_ADMIN_PASSWORD=tajnehaslo
